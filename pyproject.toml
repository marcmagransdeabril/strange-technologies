[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "live: tests that require optional/heavy dependencies (tenseal, pymc, dowhy, etc.)",
]
# Each code dir is added to sys.path so scripts can be imported directly
addopts = "--import-mode=importlib"
